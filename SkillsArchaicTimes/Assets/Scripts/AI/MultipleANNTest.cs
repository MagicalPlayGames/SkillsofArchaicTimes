using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MultipleANNTest : MonoBehaviour
{
    //Spawn Diff Enemies, destroy and read info, set the next one 
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
